<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="NextAiVPN.UI.Trial.TrialFirstChips" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:trial="clr-namespace:NextAiVPN.UI.Trial" Visibility="{Binding ControlVisibility, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
  <Border Height="32" Background="{StaticResource Color2D2D2D}" BorderBrush="{StaticResource Color3A3A3A}" BorderThickness="1" CornerRadius="15">
    <StackPanel Margin="10,0" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
      <TextBlock FontSize="12" Foreground="{StaticResource White}" Text="{Binding LeftSideText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
      <TextBlock FontSize="12" Foreground="{StaticResource White}" Text=" · " Visibility="{Binding MiddleTextVisibility, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
      <TextBlock FontSize="12" Foreground="{StaticResource White}" Text="{Binding RightSideText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </StackPanel>
  </Border>
</UserControl>